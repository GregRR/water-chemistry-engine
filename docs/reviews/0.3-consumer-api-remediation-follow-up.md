# Water Chemistry Engine 0.3 Consumer API — Remediation Follow-Up

This record summarizes the independent follow-up review of the exact range
`4cab4f2135feeadad352e2d2d8be242eb4272fb3..c8518550b11487fe2ca6ab2b6840e3947666230c`.
The remediation commit was one commit directly on top of the original
consumer-API candidate. The reviewer used a fresh, fast-forwarded clone and
confirmed the exact candidate SHA before reviewing it.

The reviewer returned **PASS**. All four findings from the original Pass 1 and
Pass 2 reviews were independently verified as closed, and no new defect or
optional improvement was identified.

This is a point-in-time audit artifact, not a normative specification. It
clears this consumer-facade implementation checkpoint only; the later 0.3
public-input/provenance and generalized treatment-ingredient authoring
decisions remain open.

## Finding closure

- **F1 / P1 — closed:** the package-root facade now exports the complete type
  graph reachable from the supported forward results. The reviewer traced
  every field to an exported type or a supported builtin/FermUnits quantity,
  then exercised resolved and unresolved result paths from an installed wheel
  without private imports.
- **F2 / P2 — closed by adjudication:** source and package metadata deliberately
  remain at 0.2.0 until the release-version gate. Consumer documentation now
  states that `__version__` is distribution identity rather than a capability
  probe for an unreleased Git checkout, and it directs consumers away from a
  moving `main` dependency.
- **F3 / P3 — closed:** project-structure documentation now accurately describes
  the selected input facade and complete forward-result audit graph while
  preserving later contract decisions as deferred work.
- **F4 / P3 — closed:** an automated test parses the marked public-API inventory
  in `docs/CONSUMER_API.md` and requires exact parity with both the independent
  expected export set and package `__all__`.

## Dependency-boundary verification

The reviewer independently installed public `ferm-units==0.1.3` without a
cache and confirmed that its package root exports `Quantity`, that `Q_`
constructs genuine Pint quantities, and that FermUnits owns the bounded Pint
dependency. No direct Pint imports remain in engine source or tests.

The rebuilt engine wheel metadata and sdist metadata each declare exactly one
runtime dependency, `ferm-units>=0.1.3,<0.2.0`. A clean Python 3.11 wheel
installation outside the checkout resolved FermUnits 0.1.3 and successfully
ran the complete documented calculation while inspecting its audit and
preparation-instruction results.

## Regression and quality evidence

The reviewer independently confirmed that:

- unknown source chemistry remains unknown through blending, treatment, and
  contribution-matrix output;
- the remediation changed quantity import boundaries without changing the
  scientific calculations;
- the documented calcium result remains unchanged;
- the consumer inventory, result discrimination, contribution matrices, and
  preparation instructions are usable solely through package-root imports;
- pH documentation accurately distinguishes FermUnits 0.1.3 capabilities from
  the engine pH contracts that have not yet migrated;
- no existing test was removed or renamed between the original and remediation
  candidates;
- all 389 tests passed locally on Python 3.11, 3.12, and 3.13, and GitHub
  Actions passed on the exact remediation commit across Python 3.11 through
  3.14 plus the quality/build job; and
- lockfile, Ruff formatting and lint, strict mypy, compilation, and whitespace
  checks passed.

## Verdict in context

The first 0.3 package-root consumer facade is complete, internally consistent,
and externally verified at commit
`c8518550b11487fe2ca6ab2b6840e3947666230c`. The project may proceed to the
next bounded 0.3 development slice. This verdict does not pre-approve future
public API, provenance, ingredient-authoring, scientific-model, or release
work; those require their own implementation and review gates.

# Water Chemistry Engine 0.5 Manual Volume-Dosing Review

This record summarizes the independent review of range
`72ec1f8..6de8789`, covering manual volume-dose resolution for exact
mass-fraction aqueous solutions, the package-root forward workflow, and the
optimizer rejection boundary. The reviewer returned the verdict **READY TO
COMMIT**, with no finding at any severity.

This is a point-in-time audit artifact, not a normative specification. Current
behavior is defined by the code, tests, and active design documentation.

## Validation performed

The reviewer reported:

- `uv lock --check`: passed;
- `uv sync --dev --locked`: passed, 24 packages audited;
- `uv run pytest`: 631 passed;
- `uv run ruff format --check .`: passed, 96 files formatted;
- `uv run ruff check .`: passed;
- `uv run mypy --strict src`: passed for 32 source files; and
- `git diff --check`: passed.

## Confirmed behavior

The review independently verified:

- exact active mass fraction, density, density reference temperature, positive
  dose increment, and measurement temperature are all required before a
  volume dose can resolve;
- solution mass is calculated as measured volume times density and active
  chemical mass as solution mass times the exact mass fraction;
- equivalent density, volume, and temperature units normalize correctly,
  including `kg/L`, liters, and degrees Fahrenheit;
- zero, negative, nonfinite, and dimensionally invalid inputs fail closed;
- measurement temperature is compared with the density reference temperature
  after unit conversion, using only a narrow floating-point-noise tolerance;
- a real temperature mismatch is rejected because thermal density correction
  is unsupported;
- density and reference temperature are never inferred or defaulted;
- composition and density provenance remain separate from one another and
  inert in the calculation;
- the resolved result retains the measured volume, measurement temperature,
  solution mass, active chemical mass, ordinary treatment addition, and
  operator-facing preparation text;
- the resolved active addition produces independently verified calcium and
  chloride results through the package-root forward API; and
- volume-dosed materials remain manual-only and are explicitly rejected as
  optimizer material constraints.

The reviewer also confirmed that the change does not infer density from water,
conflate solution form with chemical hydration state, apply a silent thermal
correction, collapse ranged composition to an exact value, enable volume-based
optimizer decisions, introduce carbonate speciation, or calculate
working-water pH.

No remediation was required.

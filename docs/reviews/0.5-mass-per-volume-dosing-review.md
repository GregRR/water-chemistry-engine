# Water Chemistry Engine 0.5 Mass-per-Volume Dosing Review

This record summarizes the independent review of range
`8db0afb..ba2df14`, covering manual volume-dose resolution from an exact
active-chemical mass-per-solution-volume concentration. The reviewer returned
the verdict **CLEARED**, with one low-severity test-coverage finding and no
correctness, unit, fail-closed, or public-API safety defect.

This is a point-in-time audit artifact, not a normative specification. Current
behavior is defined by the code, tests, and active design documentation.

## Validation performed

The reviewer reported:

- `uv lock --check`: passed;
- `uv sync --dev --locked`: passed, 24 packages audited;
- `uv run pytest`: 650 passed;
- `uv run ruff format --check .`: passed, 97 files formatted;
- `uv run ruff check .`: passed;
- `uv run mypy --strict src`: passed for 32 source files; and
- `git diff --check`: passed.

## Confirmed behavior

The review independently verified:

- the material basis matches IUPAC mass concentration: constituent mass
  divided by mixture volume;
- active chemical mass is calculated as measured solution volume times exact
  active mass concentration;
- equivalent mass-per-volume, volume, and temperature units normalize
  correctly;
- invalid, negative where prohibited, nonfinite, and dimensionally invalid
  inputs fail closed;
- measurement temperature must match the concentration reference temperature
  after unit conversion, with only a floating-point-noise tolerance;
- temperature mismatch is rejected without interpolation or correction;
- density and total solution mass are structurally absent rather than inferred;
- concentration-source provenance remains separate and calculation-inert;
- the resolved result retains the complete supported manual-dose record and
  accurate operator-facing text;
- the package-root forward workflow produces independently verified calcium
  and chloride results; and
- the new material remains manual-only and is explicitly rejected by the
  optimizer.

The reviewer also confirmed that sourced IUPAC terminology is distinguished
from the project's conservative temperature-match policy and that the change
does not select a ranged representative value, enable volume-dose
optimization, introduce carbonate speciation, or calculate working-water pH.

## Finding and adjudication

### F1 — Low — two wrong-dimension volume paths lacked direct tests

**Confirmed and remediated.** The implementation already rejected a mass-valued
volume-dose increment and mass-valued measured volumes through the shared
volume normalizer, so this was not a functional defect. Regression tests now
directly cover a non-volume increment on the mass-per-volume material and a
non-volume measured dose on both manual volume-dose material types.

No new defect was found.

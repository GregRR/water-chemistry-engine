# Water Chemistry Engine 0.5 Total Alkalinity — Remediation Follow-Up

This record summarizes the independent follow-up review of range
`d2fcda4..79e6c87`. The reviewer verified the remediation of all findings from
`0.5-alkalinity-pass-1.md`, ran the complete project quality gate, and returned
the verdict **CLEARED**.

This is a point-in-time audit artifact, not a normative specification. Current
behavior is defined by the code, tests, and active design documentation.

## Validation performed

The reviewer reported:

- `uv lock --check`: passed;
- `uv run pytest`: 569 passed;
- `uv run ruff format --check .`: passed, 93 files formatted;
- `uv run ruff check .`: passed;
- `uv run mypy --strict src`: passed for 32 source files; and
- `git diff --check`: passed.

## Finding closure

### F1 — equivalent-mass precision and provenance

**Closed.** The reviewer independently recomputed the old and new 100 mg/L
conversion examples, confirmed the changelog warning names the affected helper,
and verified that the source comments, function equation, constant, and pinned
formula-mass regression test consistently use 50.04345 g/eq.

### F2 — contribution-matrix branch coverage

**Closed.** The reviewer verified direct coverage of zero-volume unknown,
positive-volume unknown, and positive-volume known source cells. The test also
proves that unknown starting alkalinity keeps blend/final totals unresolved
while known source and sodium-bicarbonate contributions remain auditable.

### F3 — negative modeled alkalinity disposition

**Closed by adjudication.** The reviewer confirmed that retaining signed modeled
output is consistent with the active design, that negative results are
unreachable under the currently implemented nonnegative inputs/additions, and
that the review record does not overclaim future acid/base behavior.

### F4 — above-target and numerical-boundary coverage

**Closed.** The reviewer verified positive signed deviation above an alkalinity
range and direct execution of the 1e-9 mg/L floating-point boundary-tolerance
path.

## Scope verification

The reviewer confirmed that remediation did not change optimizer, alkalinity
balance, contribution-matrix, target-comparison, or notice implementation code.
Sodium bicarbonate remains excluded from automatic optimization; alkalinity is
not converted to bicarbonate/carbonate; and calculated working-water pH remains
unsupported.

No new defect was found. The first conservative-equivalent total-alkalinity
checkpoint is cleared for dependent 0.5 development.

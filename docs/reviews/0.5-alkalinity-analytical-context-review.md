# Water Chemistry Engine 0.5 Alkalinity Analytical-Context Review

This record summarizes the independent review of range
`16aa2b3..fcc8523`, covering report-native alkalinity analytical context,
explicit acid-neutralizing-capacity handling, and source-alkalinity forward
notices. The reviewer returned the verdict **CLEARED WITH LOW-RISK FOLLOW-UP**,
with no scientific, calculation, or public-contract defect.

This is a point-in-time audit artifact, not a normative specification. Current
behavior is defined by the code, tests, and active design documentation.

## Validation performed

The reviewer reported:

- `uv lock --check`: passed;
- `uv sync --dev --locked`: passed, with 24 packages audited;
- `uv run pytest`: 673 passed;
- `uv run ruff format --check .`: passed for 100 files;
- `uv run ruff check .`: passed;
- `uv run mypy --strict src`: passed for 32 source files; and
- `git diff --check 16aa2b3 fcc8523`: passed.

## Confirmed behavior

The review independently verified:

- optional preservation of total-alkalinity-versus-ANC identity, sample
  filtration state, original analyte/unit labels, analytical method and code,
  titration endpoint, and reported statistic without manufactured metadata;
- rejection of empty or incorrectly typed analytical context;
- rejection of source-only alkalinity metadata by `TargetWaterProfile`;
- backward-compatible resolution when no explicit result identity is supplied;
- preservation but fail-closed non-resolution of explicitly identified ANC,
  including conservative propagation through blending and optimization;
- stable `ACID_NEUTRALIZING_CAPACITY_UNSUPPORTED` and
  `LABORATORY_TITRATION_NOT_SIMULATED` diagnostics;
- source-alkalinity midpoint and unresolved-result notices with stable source
  identity and reasons;
- quiet handling of omitted alkalinity and unresolved zero-volume sources; and
- no bicarbonate/carbonate derivation, carbonate speciation, calculated pH, or
  unrelated treatment-material change.

## Finding and adjudication

### F1 — Low — combined ion-and-alkalinity notice coverage

**Accepted and remediated.** Production code appends alkalinity notices after
the existing per-source ion-notice loop, so inspection found no shared state or
suppression defect. The original tests nevertheless exercised ion and
alkalinity notices only in separate scenarios.

`test_ion_and_alkalinity_source_notices_coexist_without_interference` now uses
one positive-volume source with a qualified sulfate range and an exact-ended
alkalinity range under midpoint permission. It proves that the unresolved-ion
warning and alkalinity-midpoint assumption coexist in deterministic order and
retain their distinct codes, levels, reasons, ion applicability, source index,
and source name.

The review mentioned and immediately retracted an unrelated treatment-volume
test concern after confirming that it did not apply to this change range. It is
therefore not recorded as a finding.

No production-code remediation was required.
